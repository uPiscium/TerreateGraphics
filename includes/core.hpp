#include "defines.hpp"

namespace GeoFrame {
class GeoFrame {
  private:
    // Set<Unique<ResourceManager *>> mResourceManagers;

  public:
    GeoFrame();
    ~GeoFrame();

    void Run();
};
} // namespace GeoFrame
